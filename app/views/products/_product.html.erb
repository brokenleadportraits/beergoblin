
<div class="col-md-3 col-sm-12" id="beer-col">
  <!-- <div class="beer-logo" id="brooklyn"> -->
    <!-- <img src="images/brooklyn-logo.png" class="img-responsive" id="beerlogo1"> -->
 <!--  </div> -->
  <div class="beer-img-wrap">
    <%= image_tag(product.image_url, class: "img-responsive") unless product.image_url.blank? %>
    <p><%= product.name %>
    <%= product.description %><br>
    $<%= product.price %></p>
    <%= link_to 'Show', product %>
    <%= link_to 'Edit', edit_product_path(product) %>
    <%= link_to 'Destroy', product, method: :delete, data: { confirm: 'Are you sure?' } %>
  </div>
</div>